source("rv/scripts/rvr.R")
source("rv/scripts/activate.R")

options(
  repos = c(
    CRAN = sprintf(
      "https://p3m.dev/cran/2025-11-25/bin/linux/manylinux_2_28-%s/%s",
      R.version["arch"],
      substr(getRversion(), 1, 3)
    )
  )
)
